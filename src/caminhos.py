from pathlib import Path

CAMINHO_DADOS = Path(__file__).resolve().parent.parent / "data" / "dados_cartola"
CAMINHO_RANKING = Path(__file__).resolve().parent.parent / "data" / "ranking_liga"
CAMINHO_RESULTADOS = (
    Path(__file__).resolve().parent.parent / "data" / "resultados_confrontos"
)
CAMINHO_RESULTADOS_COPA = (
    Path(__file__).resolve().parent.parent / "data" / "resultados_copa"
)
